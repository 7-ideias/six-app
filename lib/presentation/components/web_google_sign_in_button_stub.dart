import 'package:flutter/widgets.dart';

/// Stub used on non-web builds. Returns an empty widget because the web-only
/// Google Identity Services button is not applicable here.
Widget renderWebGoogleSignInButton() => const SizedBox.shrink();
